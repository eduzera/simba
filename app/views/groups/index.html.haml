- title "Groups"

%table
  %tr
    %th Name
    %th Polarity
  - for group in @groups
    %tr
      %td= group.name
      %td= group.polarity
      %td= link_to 'Show', group
      %td= link_to 'Edit', edit_group_path(group)
      %td= link_to 'Destroy', group, :confirm => 'Are you sure?', :method => :delete

%p= link_to "New Group", new_group_path
